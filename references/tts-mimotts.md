# TTS and Narration

Use only when the user explicitly asks for narration or audio.

Narration should be generated from speaker text, not from raw on-slide text. Keep narration natural and explanatory.

Required files for narrated mode:

- scripts/narration.json
- scripts/tts/slide-XX.txt

Audio files are optional unless the user explicitly asks to generate audio.
